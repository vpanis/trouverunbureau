p = booking.state
p = booking.price
p = booking.space.venue.currency
p = booking.space.s_type
p = booking.quantity
p = booking.space.name
p = booking.space.venue.street.to_s + ', ' + booking.space.venue.town.to_s \
    + ', ' + booking.space.venue.country.to_s
p = booking.from
p = booking.to

- if review?(booking, own)
    p = 'Review'

- if view_receipt?(booking, own)
    p = 'View Receipt'

- if cancel?(booking, own)
    p = 'Cancel'

- if delete?(booking, own)
    p = 'Delete'
